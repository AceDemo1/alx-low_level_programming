#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print value alphabet in lower case
 * 
 * 
 * Return: Always 0 (Success)
*/

int main(void)
{
	int alpha = 'a';
	while (alpha <=z)
	{
		putchar("%c\n", alpha);
		++alpha;
	}

	return (0);
}
